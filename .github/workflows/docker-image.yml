name: Docker Image CI

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:

  build:
  
    runs-on: ubuntu-latest
    
    steps:
    # - run: curl http://ductandev.io.vn/api/cicd/update-code-server
      - name: Checkout code
        uses: actions/checkout@v2  # Sử dụng action để kiểm tra mã nguồn từ repository

      - name: Update code on server
        run: |
          ssh root@206.189.84.20 "docker exec cons-be bash -c 'git pull && exit' && docker restart cons-be"
        env:
          SSH_PRIVATE_KEY: ${{ secrets.SSH_PRIVATE_KEY }}
